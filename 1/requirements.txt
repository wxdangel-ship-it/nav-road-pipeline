# --- 基础依赖（偏 GIS / 几何 / 栅格 / 点云） ---
# 说明：
# - 本文件用于“能跑通 + 可复现”的基线环境；后续可按需要再细化拆分（e.g. requirements-gpu.txt）。
# - 若你已有更稳定的 pin，可直接替换这里的版本。

numpy==1.26.4
shapely==2.0.3
rasterio==1.4.4

pandas>=2.1,<3
pyproj>=3.6,<4
pyogrio>=0.7,<1
geopandas>=0.14,<1

laspy>=2.5,<3
tqdm>=4.66,<5
rich>=13.7,<14
pyyaml>=6,<7
orjson>=3.10,<4
